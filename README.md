# Regex Tester App

A simple Flask web app to test regular expressions.

## Features
- Enter text
- Enter regex pattern
- View matched results

## Run locally

```bash
pip install -r requirements.txt
python main.py
